Place real `.hwp` sample files in this directory for local testing.

The current repository only includes `.hwpx` samples elsewhere, which are not OLE containers and are intentionally not used by the minimal OLE dumper tests.
